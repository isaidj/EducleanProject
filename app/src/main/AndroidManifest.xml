<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.educlean">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_amarillo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_amarillo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Educlean.NoActionBar">
        <activity android:name=".activities_individual.activity_tom"></activity>
        <activity android:name=".pdf2" />
        <activity android:name=".pdf1" />
        <activity android:name=".cuidado" />
        <activity android:name=".Agua" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".Registrar" />
        <activity android:name=".Login"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Educlean.NoActionBar">

        </activity>
    </application>

</manifest>